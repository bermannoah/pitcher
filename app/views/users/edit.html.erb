<h1>Hello, <%= current_user.name %>. Edit your profile here.<h1>
  
  <%= form_for @user do |f| %>

    Name: <%= f.text_field :name %>
    Email: <%= f.text_field :email %>
    <%= f.submit %>

  <% end %>